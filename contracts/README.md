# Contracts

Solidity contracts are intentionally not implemented in this UI foundation pass.

The SRS expects a future Arc testnet proof registry using Solidity `^0.8.24` and
Foundry. This directory is reserved for that work so the app can grow into the
Arc proof anchoring phase without moving paths.
